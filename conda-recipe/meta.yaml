package:
    name: pyscan
    version: 2.1.0

build:
  number: 0

source:
    path: ..

requirements:
    build:
        - python
    run:
        - python
        - numpy
        - pyepics
        - bsread

test:
  source_files:
    - tests/

about:
    summary: "PyScan is a python class that performs a scan for single or multiple given knobs."
